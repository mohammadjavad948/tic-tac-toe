import {Socket} from "socket.io-client";
import {FC} from "react";


interface Props{
    socket: Socket
}

export const RoomSelection: FC<Props> = () => {
    return (
        <div>

        </div>
    )
}