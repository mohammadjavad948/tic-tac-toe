import {Socket} from "socket.io-client";
import {FC} from "react";

interface Prop{
    socket: Socket
}

export const Game: FC<Prop> = (prop) => {

    return (
        <div>

        </div>
    )
}

function Board(){

}