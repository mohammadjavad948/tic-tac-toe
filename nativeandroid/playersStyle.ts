import {StyleSheet} from 'react-native';

export const playerStyle = StyleSheet.create({
  container: {
    position: 'absolute',
    bottom: 0,
    right: 0,
  },
  bilbilakContainer: {
    width: '100%',
    height: 20,
  },
});
